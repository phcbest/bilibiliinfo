<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_DrawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
<!--    首页布局-->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        >
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/activity_Toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/danqianhong"
            app:titleTextColor="@color/baise"
            />
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/activity_Viewpager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            />
        <RadioGroup
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:orientation="horizontal"
            android:background="@color/danqianhong"
            >
            <RadioButton
                android:id="@+id/activity_RadioButton_hub"
                android:drawableTop="@drawable/ic_github_all"
                style="@style/activity_main_button"
                android:checked="true"
                />
            <RadioButton
                android:id="@+id/activity_RadioButton_music"
                android:drawableTop="@drawable/ic_music_all"
                style="@style/activity_main_button"
                />
            <RadioButton
                android:id="@+id/activity_RadioButton_fire"
                android:drawableTop="@drawable/ic_fire_all"
                style="@style/activity_main_button"
                />
        </RadioGroup>
    </LinearLayout>
<!--    导航栏-->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/activity_NavigationView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/activity_main_sidebar_menu"
        app:headerLayout="@layout/activity_main_sidebar_head"
        />
    
</androidx.drawerlayout.widget.DrawerLayout>
